#include <stdio.h>
/**
 * main - program start executing from main
 * Description: Display 0 - 10
 * Return: 0 (Success)
 */
int main(void)
{
int a = 0;
while (a < 10)
{
	putchar(a);
	a++;
}
return (0);
}
