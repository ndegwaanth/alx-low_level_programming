#include "main.h"
/**
 * _abs - compute the absolute value of an integer
 * @p: the character being checked
 * Return: 0 (sucess)
 */
int _abs(int p)
{
	if (p >= 0)
	{
		return (p);
	}
	return (-p);
}
