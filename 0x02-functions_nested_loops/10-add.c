#include "main.h"
/**
 * add - add two integers and the result
 * @m: int m added to n
 * @n: int n added to m
 * Return: sum of m and n
 */
int add(int m, int n)
{
	int k;

	k = m + n;
	return (k);
}
