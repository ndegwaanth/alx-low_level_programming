#include "main.h"
/**
 *  mul - multiply two integers
 *  @a: multiplied to b
 *  @b: multiplied to a
 *  Return: 0 (Success)
 */
int mul(int a, int b)
{
	int multiply;

	multiply = a * b;
	return (multiply);
}
