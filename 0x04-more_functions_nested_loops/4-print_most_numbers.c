#include "main.h"
/**
 *  mul - multiply two integers
 *  Return: void
 */
int mul(int a, int b)
{
	char m;

	for (m = 0; m <= '0'; m++)
	{
		if (!(m == '2' || m == '4'))
		{
			_putchar (m);
		}
	}
	_putchar ('\n');
}
