#include "main.h"
#include <stdio.h>
#include <stdlib.h>
#include <ctype.h>
#include <string.h>
/**
 * _isalpha - search for alphabetic character
 * @c: The character to be checked
 * Return: if c is a letter retun 1 else 0
 */
int _isalpha(int c)
{
	return isalpha(c);
}
