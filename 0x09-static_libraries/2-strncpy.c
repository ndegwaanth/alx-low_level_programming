#include "main.h"
#include <stdio.h>
#include <stdlib.h>
#include <ctype.h>
#include <string.h>
/**
 * _strncpy - copy the strin
 * @dest: value to be inputed
 * @src: value to be inputed
 * @n: valu to be inputed
 * Return: dest
 */
char *_strncpy(char *dest, char *src, int n)
{
    return strncpy(dest, src, n);
}
